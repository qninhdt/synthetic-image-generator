# @package _global_

defaults:
  - cyclegan_resnet9blocks_bdd10k

trainer:
  limit_train_batches: 0.1
  limit_val_batches: 0.1

model:
  G_net:
    ngf: 32
  D_net:
    ndf: 32

data:
  batch_size: 1
